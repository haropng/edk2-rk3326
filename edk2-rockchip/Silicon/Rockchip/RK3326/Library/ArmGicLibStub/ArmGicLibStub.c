/** @file
  Stub ArmGicLib — empty implementation for EDK2 where GIC is handled
  by ArmGicDxe (no separate library needed).
**/
#include <Library/ArmGicLib.h>
// Stub: no functions needed; SimpleInit only needs the register
// definitions from the header.
